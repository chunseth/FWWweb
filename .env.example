# Supabase (username uniqueness check against public.profiles).
# Copy to .env and fill in — the anon key only; never a service role key.
# Without these, usernames are validated locally and marked device-only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
